{
  "name": "electoral-system-blockchain",
  "version": "1.0.0",
  "description": "Blockchain audit layer for the Unified Electoral Roll & Voter Mobility System",
  "private": true,
  "scripts": {
    "node": "npx hardhat node",
    "compile": "npx hardhat compile",
    "deploy": "npx hardhat run scripts/deploy.js --network localhost",
    "test": "npx hardhat test",
    "api": "node api/server.js",
    "api:dev": "nodemon api/server.js"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.5",
    "ethers": "^6.11.1",
    "express": "^4.18.3"
  },
  "devDependencies": {
    "@nomicfoundation/hardhat-toolbox": "^5.0.0",
    "hardhat": "^2.22.2",
    "nodemon": "^3.1.0"
  }
}